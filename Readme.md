# Notetaking Rust Tool


## Usage

### Configuration

Edit the `::default()` method in [./src/config.rs](./src/config.rs) and recompile, this is easier than writing a TOML parser and handling errors at run time. Also gives tests for free.

